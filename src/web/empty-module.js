// Stub for native-only modules that aren't available on web.
// CactusRuntime and NotificationDispatcher already have try/catch
// fallbacks, so this empty export is sufficient.
module.exports = {};
