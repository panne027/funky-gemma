import { AppRegistry } from 'react-native';
import App from './App';

AppRegistry.registerComponent('NudgyNudge', () => App);
AppRegistry.runApplication('NudgyNudge', {
  rootTag: document.getElementById('root'),
});
